/**
 * 全局常量。
 *
 * 集中管理场景 key、渲染深度与逻辑分辨率，避免字符串字面量散落在各处导致拼写错误，
 * 也让深度层级的先后关系在一处即可看清。
 */

/**
 * 场景 key。
 *
 * Phaser 用字符串标识场景，直接写字面量时拼错只会在运行时暴露，
 * 因此统一从这里取用。
 */
export const SceneKeys = {
  /** 启动场景，负责最早期初始化。 */
  Boot: 'Boot',
  /** 标题场景。 */
  Title: 'Title',
  /** 世界场景，装配街区并承载主要玩法。 */
  World: 'World',
} as const

/**
 * 渲染深度层级。
 *
 * 数值越大越靠前绘制。留出间隔是为了后续能在两层之间插入新层而不必改动已有数值。
 */
export const Depth = {
  /** 天空背景。 */
  Sky: 0,
  /** 远景视差层。 */
  Far: 10,
  /** 中景建筑层。 */
  Mid: 20,
  /** 地面与行走带。 */
  Ground: 30,
  /** 场景中的可交互物。 */
  Props: 40,
  /** 角色。 */
  Player: 50,
  /** 前景遮挡层，例如近处的葡萄架、挂毯。 */
  Foreground: 60,
  /** 界面元素，始终位于最上层。 */
  Ui: 100,
} as const

/** 逻辑分辨率宽度，与 AI 出图的原生尺寸对齐。 */
export const GAME_WIDTH = 1920

/** 逻辑分辨率高度，与 AI 出图的原生尺寸对齐。 */
export const GAME_HEIGHT = 1080
